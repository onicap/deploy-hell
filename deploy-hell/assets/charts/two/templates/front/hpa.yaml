{{- if .Values.one.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "two.fullname" . }}-one
  labels:
    {{- include "two.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "two.fullname" . }}
  minReplicas: {{ .Values.one.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.one.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.one.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.one.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.one.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.one.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
